# Quick Guide

  if character == "-":
        # We subtract 1 from "asc".
    if character == "+":
        # We add 1 to "asc".
    if character == ".":
        # Prints the character.
    if character == "*":
        # Squares "asc".
    if character == "/":
        # Resets to 1.
    if character == "^":
        # I don't know but it multiplies "asc" by "asc" then subtracts "asc".
    if character == "!":
        # Debug ascii values.
    if character == ";":
        # Double "asc".

With this, you can craft wonderful creations like ASCII art, and also write stuff in an inefficient way.
